# 本轮 Join 基线源码快照

这些文件是在本轮“复用 A 表匹配键缓冲区”修改前，从工作树直接复制的
ExcelLookup 核心源码。它代表 issue #1 第一轮优化与 Codex 修正之后、但尚未
加入本轮缓冲区复用之前的实现，不是 `f6bd822` 原始版本。

- 捕获时提交：`f6bd8229501be9411983d614c39bd54921796ada`
- 捕获时 `src/join.rs`、`src/model.rs` 均来自当前工作树；工作树中的第一轮改动
  已包含在内。
- `scripts/bench-join.sh` 固定从这里编译“本轮基线”，再用同一个
  `scripts/join_bench.rs` 与当前 `src/join.rs` 对照。
- 不要让基准脚本自动覆盖本目录；需要新的优化轮次时另建快照并明确记录来源。
